<!DOCTYPE html>
<html>
  <head>
    <title>Chatapp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_importmap_tags %>

    <script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
    <script nomodule src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"></script>
  </head>


  <body>
  
    <header>
      <ul style = "list-style-type:none;" class="header"> 
        <% if user_signed_in? %> 
          <li>
          <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "header-nav-item" %>
          </li>
        <% else %>
          <li>
            <%= link_to "Sign up", new_user_registration_path, class: "header-nav-item" %>
          </li>
          <li>
            <%= link_to "Log in", new_user_session_path, class: "header-nav-item" %>
          </li>
        <% end %>
        <p class="notice"><%= notice %></p>
      </ul>
    </header>

    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
